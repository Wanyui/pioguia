{
  "name": "pioguia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:map": "esbuild src/scripts/map.ts --bundle --format=esm --platform=browser --target=es2020 --outfile=public/scripts/map.js --loader:.png=file --loader:.svg=file --asset-names=assets/[name]-[hash]",
    "predev": "npm run build:map",
    "dev": "astro dev",
    "prebuild": "npm run build:map",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@types/leaflet": "^1.9.21",
    "esbuild": "^0.27.3",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.4.5"
  }
}
